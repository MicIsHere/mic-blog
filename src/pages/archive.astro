---
import ArchivePanel from "@components/ArchivePanel.svelte";
import I18nKey from "@i18n/i18nKey";
import { i18n } from "@i18n/translation";
import MainGridLayout from "@layouts/MainGridLayout.astro";
import { siteConfig } from "../config";
import { getSortedPostsList } from "../utils/content-utils";

const sortedPostsList = await getSortedPostsList();
const archiveTitle = `${i18n(I18nKey.archive)}、分类与标签浏览`;
const archiveDescription = `${siteConfig.title}的文章归档页面，可按发布时间、分类和标签浏览技术教程、踩坑记录、日常折腾与动漫相关内容。`;
---

<MainGridLayout title={archiveTitle} description={archiveDescription}>
    <h1 class="sr-only">{archiveTitle}</h1>
    <ArchivePanel sortedPosts={sortedPostsList} client:only="svelte"></ArchivePanel>
</MainGridLayout>
